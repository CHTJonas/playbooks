debug = false

logLevel = "ERROR"
defaultEntryPoints = ["https","http"]
insecureSkipVerify = true

[entryPoints]
  [entryPoints.http]
  address = ":80"
    [entryPoints.http.redirect]
    entryPoint = "https"
  [entryPoints.https]
  address = ":443"
  [entryPoints.https.tls]

[retry]

[docker]
endpoint = "unix:///var/run/docker.sock"
domain = "unifi.jfis.co.uk"
watch = true
exposedByDefault = false

[acme]
email = "certs@charliejonas.co.uk"
storage = "acme.json"
entryPoint = "https"
onHostRule = true
[acme.httpChallenge]
entryPoint = "http"
